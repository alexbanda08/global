"""Pre-flight filters: conflict, hours, blowoff, OB manipulation, reentry.

Each filter is independent. The conflict filter is the only one whose
output decides direction; the rest just gate the fire.
"""
