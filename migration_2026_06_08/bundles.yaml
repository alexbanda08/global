# /etc/tv/bundles.yaml — HL V52 + XSM shadow bundles (wired 2026-06-14)
# paused: true  => all member sleeves run in SHADOW (paper, $0). Keeps V52/XSM
# paper-only per the promotion gates (>=4wk shadow, Sharpe>1.2, funding ±5%).
# Fixes the dashboard "bundle: none" on the SHADOW (6) HL cards.
# Schema (engine/bundles.py): bundle_id, venue HL|POLY, total_size_usd, paused, sleeve_ids[].
bundles:
  - bundle_id: HL_V52_SHADOW
    venue: HL
    total_size_usd: 5000.0
    paused: true
    sleeve_ids:
      - V52-BTC
      - V52-ETH
      - V52-SOL
      - V52-AVAX
      - V52-LINK
  - bundle_id: HL_XSM_SHADOW
    venue: HL
    total_size_usd: 5000.0
    paused: true
    sleeve_ids:
      - V24-XSM
