# Session Handoff — 2026-06-01 (sleeve optimization, full-period persistence, hedge research, portfolio, ema_down deep-dive)

_Pickup doc for the next session. Big multi-part research session: analyzed/optimized the live shadow sleeve fleet, ran full-period backtests on the precomputed universe panels, did internet-backed hedge/exit research, built per-market decorrelated deploy portfolios, and deep-dived btc_15m_ema50_ema800_off600_down. **Start at §"Next-session pickup" + §"Open items".**_

---

## TL;DR — the 6 findings that matter

1. **Engine is faithful; live ≫ backtest is overfit-decay.** The recurring theme: short-live-window numbers are inflated vs full-period OOS.
2. **Every time-of-day/session gate failed OOS.** `keep_EU` (kelly), `drop_US` (sol_rf, ETH), `vsum` — all looked great in the 3-5 day live window, all **failed the full-period (Apr24-May26) OOS test**. The signals that **persist** are conviction/momentum/price: **`fair_edge_bp>1000`** (kelly), **`ma_300` binance momentum** (sol_rf), **`entry_vwap≤0.70`** (marginal sleeves), and the **ETH 5m directional stack**.
3. **ETH 5m sniper family is the real, low-drawdown alpha** — `l_ema50_hurst_grandparent_v8`, `bb_mp_hurst_v6`, `cloud_ribbon/cloud_vwap` — full-period WR 72-82% ≈ live, Calmar 10-17. The fleet's best.
4. **Exit/hedge: fixed SL/TP/trailing all lose. The ONE robust hedge is `HEDGE_LATE`** (final 30s, sell if bid<0.6×entry) and **only on MARGINAL sleeves** (`parent15m_notrang` +$45, CI-lo +0.15). Don't hedge winners; don't rescue losers.
5. **2 KILLs confirmed dead full-period** (51% WR causal): `btc_5m_q_parent15mslope_ts_imb5_v8` (−$930) + `btc_5m_ts_mpskew_any_off30` (−$93). Originals were look-ahead.
6. **`btc_15m_ema50_ema800_off600_down`: the cheap-entry "edge" is the 0.15-0.70 contrarian zone, NOT the extremes.** Deepest-lottery (<0.15) and no-upside favorites (≥0.95) are both −EV. **Band `[0.15, 0.93]` = +32% total** (full-period +$176→+$231). Also runs on Kalshi.

---

## Operator deploy decisions made this session
- **ema_down V10 band `[0.15, 0.93]`, $1/fire**: → **LIVE on Kalshi** (with band) + **Polymarket SHADOW** (with band); Polymarket parent (no band) keeps running live as the same-venue A/B baseline. Spec: `TV_AGENT_SPEC_EMA_DOWN_V10_2026_06_01.md`. **(User is deploying — not yet registered.)**
- Note: clean band A/B = same-venue (Poly parent live vs Poly V10 shadow). Kalshi-vs-Poly is cross-venue (confounded) — don't measure the band from it.

---

## What was done (chronological)

1. **Reconnected VPS3** (185.190.143.7). Tool sandbox blocks outbound port 22 → **must use Bash with `dangerouslyDisableSandbox: true`** for ssh/scp. Refreshed canonical `trading_events` (now fresh to **Jun 1 11:04 UTC**, ~1.42M events; user later added futures collectors + L25 topoff).
2. **21-sleeve optimization** → `SLEEVE_OPTIMIZATION_2026_05_30.md`: live-vs-backtest, walk-forward gate sweep, bootstrap-CI gated configs, external-feature gates. Deploy-ready gates (CI-lo>0): `sol_cci evcap+drop_US`(+0.33), `eth_l_ema50 evcap≤0.70`(+0.25), `sol_rf drop_US`(+0.16), etc.
3. **Hedge/exit research** → 3 internet researchers (`RESEARCH_{BINARY_HEDGING,STOPLOSS_EXITS,DYNAMIC_HEDGING}`) + exit/hedge harness + `_H` decode → `HEDGE_EXIT_RESEARCH_SYNTHESIS_2026_05_30.md`. Topped off BTC L25 to May 31 to get the real 1-5min hold window; HEDGE_LATE robust on marginal sleeves.
4. **Full-period persistence** → `FULLPERIOD_PERSISTENCE_2026_05_30.md`: reconstructed sleeves on the `_sniper_*_universe` panels (Apr24-May26 = GA TRAINING set = in-sample). ETH base persists; `entry_vwap≤0.70` persists OOS; `drop_US`/`vsum` fail OOS; KILLs confirmed.
5. **5-strategy re-analysis + MDD/Calmar + new gates** → `ETH_NEWGATES_MDD_2026_05_31.md`, `KELLY_FULLPERIOD_2026_05_31.md`, `SOLRF_FULLPERIOD_2026_05_31.md`, `FULLPERIOD_5STRATS_FINAL_2026_05_31.md`. Kelly = leverage on weak signal, week-21-concentrated → `fair_edge_bp>1000`+½-Kelly. sol_rf base LOSES full-period (not deploy-ready; ma_300 is the only real signal).
6. **V10 specs** → `TV_AGENT_SPEC_V10_SLEEVES_2026_05_31.md` (eth_l_ema50+sms_no_liq, eth_cloud_ribbon+tr_above_pp, eth_bb+narrow_band, kelly fe1000+½-Kelly).
7. **Consolidated tables** → `MASTER_FINDINGS_TABLE_2026_05_30.md`, `ALL_RESULTS_TABLE_2026_05_31.md` (every test: WR/$tr/n/period).
8. **Portfolio by market** → `DEPLOY_PORTFOLIO_BY_MARKET_2026_06_01.md`: 202 active sleeves → 43 qualify (WR>55, +total). **2 variance classes** (momo std~24 / sniper std~3-8). Redundancy clusters + decorrelated deploy set per market.
9. **ema_down deep-dive** → `EMA_DOWN_DEEPDIVE_2026_06_01.md` + `TV_AGENT_SPEC_EMA_DOWN_V10_2026_06_01.md`: fidelity ✅, entry-bucket edge map, full-period backtest, band [0.15,0.93].

---

## Artifacts

**Reports** (`strategy_lab/reports/`): SLEEVE_OPTIMIZATION_2026_05_30, GATED_CONFIGS_2026_05_30, EXTERNAL_GATES_2026_05_30, RESEARCH_{BINARY_HEDGING,STOPLOSS_EXITS,DYNAMIC_HEDGING}_2026_05_30, HEDGE_EXIT_RESEARCH_SYNTHESIS_2026_05_30, FULLPERIOD_PERSISTENCE_2026_05_30, MASTER_FINDINGS_TABLE_2026_05_30, ETH_NEWGATES_MDD_2026_05_31, KELLY_FULLPERIOD_2026_05_31, SOLRF_FULLPERIOD_2026_05_31, FULLPERIOD_5STRATS_FINAL_2026_05_31, ALL_RESULTS_TABLE_2026_05_31, TV_AGENT_SPEC_V10_SLEEVES_2026_05_31, DEPLOY_PORTFOLIO_BY_MARKET_2026_06_01, EMA_DOWN_DEEPDIVE_2026_06_01, TV_AGENT_SPEC_EMA_DOWN_V10_2026_06_01.

**Scripts** (`strategy_lab/_opt_2026_05_30/`): 01-25 (`01_live_stats` … `25_ema_down_band`). Key: 02 substrate, 03/04 gate sweep+walkforward, 06/07/08 exit-hedge, 14 fullperiod persistence, 16 eth newgates+MDD, 20/21 portfolio cluster, 23/24/25 ema_down.

**Result data** (`strategy_lab/_opt_2026_05_30/_results/`): fires_resolved_all.parquet, all_sleeve_fires.parquet, {gate_sweep, walkforward_gates, final_gated_configs, external_gates, hedge_late_sweep, fullperiod_base, fullperiod_gate_persist, qualifying_with_variance}.csv, exit_grid_BTC_fresh.parquet, **btc_l25_topoff.parquet** (BTC L25 May29-31 overlay — kept for hedge re-runs).

---

## Open items (nothing half-broken; these are deploy actions + validation gaps)

**A. Specced, not deployed (VPS3 / tv-agent):**
- ema_down V10 band [0.15,0.93] (Kalshi live + Poly shadow) — operator deploying.
- 4 V10 sleeves (ETH×3 + kelly fe1000+½-Kelly).
- Gate changes (`entry_vwap≤0.70` etc.) + **2 KILLs not executed** (btc_5m_q, btc_5m_ts still running).
- HEDGE_LATE on marginal sleeves (parent15m_notrang, vwapprem) — finding, no deploy spec yet.

**B. Validation gaps:**
- **No SOL universe panel** → `sol_cci/f7_mfi/btcf7/j` never full-period OOS-tested; `sol_rf` only approximate. **Build a SOL 5m universe** (dirscan_sol_5m + range_filter_1s + traders_reality_1s + SOL L25 + binance ma_300) — biggest gap.
- **`btc_5m_parent15m_notrang` reconstruction broken** (missing `parent_15m_not_ranging` gate → 28× over-fire) → full-period + its +$45 HEDGE_LATE unconfirmed.
- New ETH gates (`g_sms_no_liquidity_above` etc.) are **in-sample only** — V10 shadow A/B will confirm.
- Exit/hedge tested **BTC only** (fresh L25 was BTC) — ETH/SOL untested.
- Kelly only **May 1-21** (panel limit); edge week-21-concentrated.

**C. Data hygiene flagged:**
- **HL liquidations feed stale** (ends May 27) → HL-cascade gates untestable.
- **Frozen ETH chainlink basis (14.93 bps)** → likely stale CL feed; investigate.

**D. Quick follow-ups offered, not run:**
- `ema_down` vs `ts_trstack_off600_down` Jaccard overlap (redundant?).
- Entry-band [0.15,0.93] edge-map on other DOWN sleeves (ts_trstack, vwapprem).
- V9 polyflow `b1` sleeves full-period (`sol_5m_b1_polyflow_aligned`, `down_b1_500` — high ppd, tiny n).
- **`poly_mint_sell_btc_5m`** (+$1,924, 7,899 trades) — biggest qualifier, never analyzed (maker strat; CLAUDE.md flags maker-arb censoring caution).

---

## Environment / gotchas (read before coding)
- **VPS3 ssh/scp:** Bash tool with `dangerouslyDisableSandbox: true` (sandbox blocks port 22). `sudo -u postgres psql -d storedata`. `\copy` writes as postgres → `chmod 777` the /tmp dir first.
- **Python:** `C:/Python314/python.exe` (pandas 2.3.3, pyarrow). The **ctx_execute sandbox python is NOT installed** — run python via `ctx_batch_execute` shell with the full path, or Bash.
- **pandas `.at` gotcha:** `r.at` / `df.at` is the indexer, not a column — use `r["at"]` / `df["at"]`. Bit me 3×.
- **Fee model = 0.07-curve** (`pnl_won=(1-vwap)·shares·(1-0.07·vwap)`), NOT legacy 2%. Operator-confirmed.
- **Universe panels (full period Apr24-May26, gates+fills+outcomes precomputed):** `_sniper_eth5m_v6/v7/v8_universe`, `_sniper_btc_5m_enriched`, `sniper_btc15m_v8_gated` (has the ema_down substrate: direction/won/entry_vwap/g_tr_above_ema50/ema800). **These are the GA TRAINING set → in-sample.** `dirscan_{sol,btc}_5m/15m` = fire universe with fills but fewer gates. **No SOL 5m sniper universe.**
- **`_H` schema decode:** resolution `fire_us` = slot_end (resolution boundary); entry = slot_start + fire_offset_s. Real hold = window − offset (1-5 min), NOT 20s.

---

## Next-session pickup (ranked)
1. **Confirm the ema_down V10 + 4 V10 sleeves got registered** (or register them). Execute the 2 KILLs.
2. **Build the SOL 5m universe** → closes the biggest validation gap (sol_cci/f7_mfi/btcf7 full-period + the sol_rf/drop_US/ma_300 OOS question).
3. **Fix `btc_5m_parent15m_notrang` reconstruction** (add `parent_15m_not_ranging`) → confirm its +$45 HEDGE_LATE full-period.
4. **Watch the V10 shadow A/Bs** (7-day): ETH new gates + ema_down band — promote only if V10 ≥ parent on $/tr + Calmar.
5. Data hygiene: refresh HL liqs, investigate ETH CL basis freeze.
6. Quick wins (D): ts_trstack overlap, band-map on other DOWN sleeves, V9 polyflow full-period, mint_sell analysis.

State: **fleet analyzed end-to-end; ETH 5m sniper family + ema_down(band) are the highest-confidence deploys; session/ToD gates are overfit; KILL the 2 confirmed losers; SOL needs a universe build to finish validation.**
