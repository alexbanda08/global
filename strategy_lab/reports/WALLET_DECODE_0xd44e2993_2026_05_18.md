# Wallet decode: `0xd44e29936409019f93993de8bd603ef6cb1bb15e` (short `0xd44e2993`)

**Author:** subagent, **Date:** 2026-05-18
**Method:** indirect (Option B) — no Alchemy key available in env, no `.env` file at repo root.
**Status:** PARTIAL DECODE (lower bound from 5 taker wallets' on-chain caches)

---

## 1. How the data was obtained

Indirect — scanned `trades_chain.parquet` of every taker wallet in
`strategy_lab/wallet_hunt/cache/` for rows whose `maker` field
(case-insensitive lowercase) equals
`0xd44e29936409019f93993de8bd603ef6cb1bb15e`.

Per-source breakdown:

| taker wallet | rows scanned | rows where `maker == 0xd44e2993` |
|---|---:|---:|
| `0x04b6d7e9` |  57,415 |     12 |
| `0x89b5cdaa` | 112,296 |      0 |
| `0xce25e214` | 208,599 |  9,396 |
| `0xcfb103c3` |  48,379 |      0 |
| `0xeebde7a0` | 547,545 |  5,953 |
| **TOTAL** | **974,234** | **15,361** |

Dedupe by `(tx_hash, log_index)` removed 0 duplicates — counterparty matches are
disjoint across the 5 taker chains, as expected. 15,361 trades is the **lower
bound** for d44's activity in the window we have. Most of d44's counter-trades
are with `0xce25e214` (61%) and `0xeebde7a0` (39%); virtually none against
the other 3.

> **Limitation:** the true universe is larger. d44 also trades against takers
> we don't have cached (notably the F2 cluster and HFT scalpers).
> Numbers below are observed lower bounds. Real daily volume is likely
> 2–5× the figure here once unseen takers are added.

Intermediate artifacts (kept for next-session reuse, will not be committed):

- `strategy_lab/wallet_hunt/cache/_d44e2993_indirect.parquet` (15,361 rows)
- `strategy_lab/wallet_hunt/_d44_analyze.py` (one-shot stats script)
- `strategy_lab/wallet_hunt/_d44_extra.py` (two-sidedness + baselines)

---

## 2. Maker confirmation

**Confirmed PURE MAKER.** All 15,361 observed trades have
`maker == 0xd44e2993` by construction and `side == 'BUY'` (i.e. our taker
was the buyer → d44 posted a **SELL limit**) for **100.0% of fills**.
`side_uint == 0` for 100% (Polymarket UpDown order schema —
0 = maker BUY token / sells the complementary token, but our taker side is BUY,
so d44's posted order is the resting SELL).

There is **no taker-side activity** of `0xd44e2993` visible in any
counter-wallet's chain. Pure maker.

---

## 3. Scale

### Observed window

| field | value |
|---|---|
| Time span (UTC)            | 2026-05-10 05:45 → 2026-05-16 09:21 |
| Span (days)                | 6.15 |
| Trades observed            | 15,361 |
| Trades joinable to slug    | 13,737 (89.4%) |
| Unique slugs touched       | 1,084 |
| Total notional             | **$35,779** |
| **Daily volume (observed)** | **$5,818 / day** |
| Daily slugs                | 176.3 / day |

### Orphans (10.6% — slug join failed)

~1,624 rows / $1,948 notional did not match the canonical token lookup. The
top 5 orphan asset_ids each have 19–31 fills. These are likely non-updown
markets or markets minted after the last token-lookup refresh.

### Daily breakdown

```
date           fills    USDC    slugs
2026-05-10     1,123  $1,232    207
2026-05-11     2,944  $8,464    292
2026-05-12     3,508  $8,652    439
2026-05-13     2,258  $5,647    373
2026-05-14     1,447  $3,003    390
2026-05-15     3,339  $7,950    501
2026-05-16       742    $830    176  (partial day)
```

### Hour-of-day

Consistent activity 24/7 with mild peaks at 01–05 UTC (Asia evening / EU
morning) and a quieter window 12–16 UTC (US open). No big gaps. Behaves
like an autonomous bot.

---

## 4. Per-slug behaviour

| metric | median | p25 | p75 | mean | max |
|---|---:|---:|---:|---:|---:|
| **fills / slug**  | 9.0 | 5.0 | 17.0 | 12.7 | 94 |
| **shares / slug** | 50.3 | 25.0 | 107.6 | — | — |
| **USDC / slug**   | $13.53 | $6.09 | $36.41 | $31.21 | — |
| **size / fill**   | 5.00 | 5.00 | 10.00 | 6.41 | — |
| **price / fill**  | 0.270 | 0.163 | 0.432 | — | — |
| **USDC / fill**   | $1.50 | $0.86 | $3.30 | $2.46 | — |
| **dur per slug (min)** | 8.7 | 4.5 | 11.6 | — | — |

**Two-sided behaviour (mint-and-sell signature):**
- BOTH Up *and* Down posted in same slug: **880 / 1,084 = 81.2%** ✅
- Only Up:   113 (10.4%)
- Only Down:  91 ( 8.4%)
- Up fills:  6,909 ; Down fills: 6,828 — essentially symmetric.

**Price-band:** d44 posts SELL limits in the LOW band (median 0.27) on
both Up and Down legs simultaneously. This is the *classic mint-and-sell
signature*: mint a complete share pair (cost $1.005), then post both legs
as resting sells at a low ask, collect ~$0.04 in slug duration if one
side touches before resolution.

**Fill timing inside slot:** median **480 s after slot start** (range
244–684 s = p25–p75). Slot is 900 s, so fills cluster in the **middle
third of the slot** — d44's limits sit through the early "quiet" first 4
min, then get touched by takers around the 5–11 min mark when the
fast/sniper takers start probing the book.

---

## 5. Cell focus

| asset | timeframe | fills | USDC | slugs | % fills | % USDC |
|---|---|---:|---:|---:|---:|---:|
| BTC | updown_15m | 8,644 | $27,862 | 519 | 63.4% | **82.6%** |
| ETH | updown_15m | 2,838 |  $3,329 | 284 | 20.8% |  9.9% |
| SOL | updown_15m | 2,160 |  $2,538 | 263 | 15.8% |  7.5% |
| (5m anywhere) | — | **0** | $0 | 0 | 0% | 0% |

**Per-cell per-slug:**

- BTC/updown_15m: 519 slugs, median 14 fills/slug, median $35.31/slug, total $27,862
- ETH/updown_15m: 284 slugs, median  7 fills/slug, median  $8.60/slug, total  $3,329
- SOL/updown_15m: 263 slugs, median  6 fills/slug, median  $7.14/slug, total  $2,538

**Conclusion:** **15-minute updown only**. BTC dominates by notional
(83%), ETH/SOL are kept active in smaller size — probably for inventory
diversification or to lock in maker rebates across cells. **Zero 5-minute
exposure** — a deliberate choice that sharply distinguishes d44 from the
other three mint-and-sell makers.

---

## 6. Comparison vs the 3 known mint-and-sell wallets

Daily-volume and per-slug numbers below are recomputed from each wallet's
own `trades_chain.parquet` over the **same kind of window length** (each
wallet's full visible chain). Maker% comes from the wallet's own self-view
(when wallet *is* the taker, `wallet_is_taker == True`).

| Wallet | span | fills/slug (median) | $/slug (median) | maker % | Cells | Daily volume |
|---|---:|---:|---:|---:|---|---:|
| `0x89b5cdaa` | 6.1 d |  10 |    $304  | 100.0% | All 6 (BTC/ETH/SOL × 5m+15m) | $1.12 M |
| `0x04b6d7e9` | 1.6 d | **197** | **$20,890** |  97.7% | **BTC 5m + 15m only** | **$13.24 M** |
| `0xeebde7a0` | 6.1 d |  79 | $4,189 |  50.2% (hybrid) | BTC + ETH × 5m + 15m | **$96.36 M** |
| **`0xd44e2993`** | 6.1 d | **9** | **$13.53** | 100.0% pure | **15m only, BTC/ETH/SOL** | **$5,818** |

**Where d44 sits on the spectrum:**

1. **Tiniest by 3+ orders of magnitude.** Median $13 per slug vs $304 / $4k /
   $20k for the others. Daily volume $5.8k vs $1M / $96M / $13M.
2. **Most fragmented fills.** Median fill is $1.50 (vs $30+ for others) —
   tiny resting size, gets hit many times in small chunks.
3. **Pure maker** (100% maker-side, never takes) — like 0x89b5cdaa,
   unlike 0xeebde7a0.
4. **Most two-sided** (81.2% slugs both-sides) — full mint-and-sell signature.
5. **Only 15m**, never 5m — unique among the four.
6. **All 3 underlying assets** (BTC/ETH/SOL) at 15m — close to 0x89b5cdaa's
   breadth but at much smaller size.

---

## 7. Verdict

| question | answer |
|---|---|
| Is it confirmed mint-and-sell maker? | **YES** — 100% maker, 81% two-sided, low-band sell limits both legs. |
| Broad or selective? | **Broad** (BTC/ETH/SOL) but **shallow** (low size, 15m only). |
| Daily scale? | ~$5.8k observed / lower bound. Real volume likely $15–30k/day after unseen takers. |
| Pure maker or hybrid? | **Pure maker** — 0% taker-side activity in any cached counter-wallet. |
| Closest peer? | **`0x89b5cdaa`** in style (broad, pure maker, two-sided), but **5× smaller** in daily $ and **only 15m**. |
| Worth full decode? | **NO** — not at this size. See below. |

### Why this wallet does NOT deserve a full decode pass

1. Daily P&L ceiling is ~$50–100/day (mint-and-sell yields a few % of
   notional per slug). Not strategically interesting next to F1/F2 / the
   other 3 makers (which are $1k–$300k/day).
2. Its strategy is already well-understood from the three existing
   decodes — it's a *scaled-down* version of `0x89b5cdaa` restricted to
   15m markets. No new alpha mechanism to learn.
3. Two-sided 81%, low-band sells, all 3 underlyings at 15m — checks every
   box of the canonical mint-and-sell template documented in
   `MINT_AND_SELL_V2_FULL_REPLICATION_2026_05_16.md`.
4. The reason it shows up as **rank #1** in our takers' counterparty
   frequency table is not its size but its **fill-fragmentation**:
   median size 5 shares means a single $25 taker-buy can clip d44's
   sleeve 5 times → 5 row entries. So the 15,361-count is inflated vs
   actual *trade events* by ~3–4×.

### What WOULD be worth chasing next session (recommended priorities)

| Priority | Wallet | Reason |
|---|---|---|
| **HIGH** | `0xb27bc932` (HFT scalper, $254k/day, uses relay `0xf3cfb6a6...` for exit) | Already in catalog as undecoded; biggest unknown after F2. |
| **HIGH** | The F2 slug-selector (needs WS tape + cross-exchange basis) — see `F2_FINAL_VERDICT_2026_05_18.md` §5 | $5.9k/day per wallet, decoded mechanics, only slug-pick remains. |
| MED | Search for makers ranked 2–5 in our takers' counterparty frequency table — may surface another big mint-and-sell (this exercise found d44 small, but there are higher-frequency *and* higher-notional candidates to check). |
| LOW | `0xd44e2993` itself — only revisit if direct Alchemy decode (full taker-side history) reveals something the indirect view missed. |

---

## Appendix: reproducibility

```python
# All numbers above can be regenerated by:
py -3 -X utf8 strategy_lab/wallet_hunt/_d44_analyze.py
py -3 -X utf8 strategy_lab/wallet_hunt/_d44_extra.py
```

These scripts read `strategy_lab/wallet_hunt/cache/_d44e2993_indirect.parquet`
which was produced by:

```python
TARGET = '0xd44e29936409019f93993de8bd603ef6cb1bb15e'
for w in ['0x04b6d7e9','0x89b5cdaa','0xce25e214','0xcfb103c3','0xeebde7a0']:
    df = pd.read_parquet(f'strategy_lab/wallet_hunt/cache/{w}/trades_chain.parquet')
    hits = df[df['maker'].str.lower() == TARGET]
    ...
```

Total runtime: ~15 seconds on the local box.
