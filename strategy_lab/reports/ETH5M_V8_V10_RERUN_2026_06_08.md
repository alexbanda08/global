# ETH 5m `l_ema50_hurst_grandparent` v8 vs v10 — Re-run + v10-gate delta (2026-06-08)

**Ask:** find the study, re-run with correct fee + ml4t rigor on current canonical, confirm
profitable, quantify the v10-gate difference.

## The sleeve + the v10 gate
Study: `strategy_lab/sniper_search_2026_05_27/eth_5m_v8/` (GA search, Apr24→May26). Sleeve def
(Ireland `sniper_v5_sleeves.py`):
- **v8** = ETH 5m BOTH, offset 60, gates: `g_tr_above_ema50` + `g_hurst_trending`(hurst_60>0.50)
  + `g_grandparent_trend_with`(15m trend_slope 4-bar mean sign = dir).
- **v10** = v8 **+ `g_sms_no_liquidity_above`** (UP: not near 20-bar swing-high liquidity; DOWN:
  not near swing-low). The ONLY difference.

## Result 1 — FAITHFUL in-sample re-run (correct 0.07 fee + DSR)
Used the existing v8 universe's EXACT gate columns (computed by the real builders) + `won` +
`entry_vwap`; repriced with the operator-confirmed **0.07 winner-only fee** (the study used the
wrong legacy 2% fee). `strategy_lab/directional/eth5m_v8_v10_faithful_2026_06_08.py`.

| | n | WR | $/tr | total | MaxDD | Calmar | DSR boot p(>0) |
|---|--:|--:|--:|--:|--:|--:|--:|
| **v8**  | 467 | 82.0% | +0.925 | +$432 | −$24.9 | 17.32 | 1.000 |
| **v10** | 353 | 83.9% | +1.006 | +$355 | −$15.0 | 23.66 | 1.000 |

- Reproduces the study exactly (Calmar 17.3 / 23.7) → fidelity confirmed.
- **Both profitable even with the correct fee.** The v10 gate (`g_sms_no_liquidity_above`):
  keeps 76% of fires (467→353), WR 82→84%, $/tr +0.93→+1.01, **MaxDD −$25→−$15 (−40%)**,
  Calmar 17.3→23.7. Trades ~¼ of volume for ~40% less drawdown + higher per-trade.
- ⚠️ **This is the GA TRAINING window (in-sample).** Not proof of forward edge.

## Result 2 — OOS on NEW data via the LIVE production gates (shadow fleet, VPS3)
The faithful "new canonical" test = the actual sleeve's resolved shadow fires (production
gate code, real books), window **2026-05-29 → 06-09 — disjoint from the GA window**.
`migration_2026_06_08/eth_grandparent_shadow_agg.py`.

| | n | WR | total | mean $/tr | avg vwap | breakeven WR |
|---|--:|--:|--:|--:|--:|--:|
| **v8**  | 628 | 67.4% | +$219.8 | +0.350 | 0.628 | ~62.8% |
| **v10** | 293 | 66.2% | +$68.8  | +0.235 | 0.627 | ~62.7% |

- **Still net-positive OOS, but WR decayed 82% → 67%** (classic in-sample inflation). v8
  clears breakeven (67.4% > 62.8%) → +$0.35/tr in shadow.
- 🔴 **The v10 in-sample improvement did NOT clearly persist OOS** — v10 mean (+0.235) < v8
  (+0.350) here (though v10's window is shorter, started 06-02). The DD-cut gate didn't add
  value forward in this sample.

## Honest caveats (do not over-read)
- **Shadow ≠ live.** Shadow uses optimistic fills + the known live↔shadow parity divergence
  (per-host feed/threshold). The real-$ version of this sleeve was **STOPPED after a small
  realized loss** (`HANDOFF_2026_06_03`, eth ≈ −$5, n≈9). Shadow-positive is NOT live-proof.
- **My from-scratch klines recompute is DISCARDED** — reproducing `hurst_60`/`ema_50`/`sms`
  offline gave WR 47.8% (coinflip), an unfaithful feature reproduction, not evidence the edge
  died. (`eth5m_v8_v10_rerun_2026_06_08.py`, kept for the record.)
- Full pre-Apr24 backfill re-run was NOT done: the production panel chain can't be cleanly
  rebuilt (SMS builder input `binance_1s_28d.parquet` was deleted in the May-27 dedup;
  hardcoded windows; universe builders use production L25 only, no backfill-book fill).

## Verdict
- **v8 + v10 are profitable in-sample with the correct fee, and the v10 gate measurably
  improves the in-sample risk profile (Calmar 17.3→23.7, MaxDD halved).** ✓ confirmed.
- **OOS (new data, production gates): still net-positive in SHADOW but the edge decays
  (WR 82→67) and the v10 gate's advantage does not clearly carry forward.**
- **Real-capital verdict remains unproven-to-negative** (live sleeve was stopped at a loss;
  parity caveat). To truly confirm: judge by the LIVE wallet over ≥100 fires, not shadow.

Scripts: `strategy_lab/directional/eth5m_v8_v10_faithful_2026_06_08.py` (in-sample, faithful),
`eth5m_v8_v10_rerun_2026_06_08.py` (klines recompute — unfaithful, discarded),
`migration_2026_06_08/eth_grandparent_shadow_agg.py` (OOS shadow).
