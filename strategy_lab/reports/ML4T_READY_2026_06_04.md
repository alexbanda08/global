# ml4t toolkit — INSTALLED & VALIDATED (Python 3.14) — 2026-06-04

## Installed (editable, on main 3.14 env — shares our data stack + GPU torch)
- `external/ml4t-engineer`  (0.1.dev, editable) — features + labeling
- `external/ml4t-diagnostic` (0.1.0b21) — DSR / PBO / CPCV / Rademacher / data-quality
- `external/ml4t-models`    (0.1.0a4) — forecasters / portfolio models
- deps pulled: polars 1.40, numba, statsmodels, arch, shap, exchange-calendars (all OK on 3.14)
- **Not installed (optional):** `ml4t/data` (we have canonical), `ml4t/backtest` (engine_v2 is our poly fill model;
  install only to backtest the underlying daily-trend survivors).

## Validated APIs (smoke-passed on real BTC 1h)
- **Features:** `from ml4t.engineer import compute_features, FeatureCatalog` — 120 features
  (`FeatureCatalog().list()`: ad, adosc, adx, atr, macd, rsi, … ). `compute_features(polars_df, [names])`.
  Input schema: polars DF with `timestamp, open, high, low, close, volume`.
- **Labels (López de Prado):** `from ml4t.engineer.labeling import atr_triple_barrier_labels, meta_labels,
  apply_meta_model, fixed_time_horizon_labels, percentile_labels, calculate_sample_weights, sequential_bootstrap,
  compute_bet_size`.
- **Deflated Sharpe:** `from ml4t.diagnostic.evaluation.stats.deflated_sharpe_ratio import deflated_sharpe_ratio`
  → `deflated_sharpe_ratio(returns, frequency=..., effective_trials=N) -> DSRResult(probability, is_significant,
  z_score, p_value, sharpe_ratio, ...)`. **`effective_trials` = the multiple-testing deflation** for our searches.
- **PBO:** `ml4t/diagnostic/evaluation/stats/backtest_overfitting.py`.
- **Rademacher:** `...stats/rademacher_adjustment.py: rademacher_complexity`.
- **CPCV:** `from ml4t.diagnostic.api import CombinatorialCV` ; purging in
  `ml4t.diagnostic.core: apply_purging_and_embargo, calculate_purge_indices` ; `ValidatedCrossValidation`.

## RUN PLAN (execute when canonical update is done)
1. **Re-judge the overnight finds under DSR (with the real trial counts):**
   - CPU scalp/slug finalists (`_data/overnight/finalists.json`, 50, searched 387,200): run each survivor's
     trade-return series through `deflated_sharpe_ratio(effective_trials≈387200)`. Almost all will fall to
     `is_significant=False` — the ones that DON'T are the real candidates.
   - Mega-sweep survivors (29 of 4.8M): same, `effective_trials≈4.8e6`. The daily-trend cluster is the only
     plausible holdout.
2. **Rebuild the poly + scalp datasets with `engineer`:** 120 features + **meta-labeling / triple-barrier** on
   the scalp/lag-taker fire universe (anchored at ws_s).
3. **Validate with `CombinatorialCV` (CPCV)** instead of the single lockbox; judge with **DSR + PBO**.
4. **Meta-label the exit-scalp** (`apply_meta_model`) + CPCV to sharpen its entry filter — the live edge.
5. **Different-window OOS** (`validate_oos.py` + 6-month API) remains the final gate.

## Note
All data assets ready: 8.8y spot + 6y futures (`_data/binance_vision[_deriv]/`), `master_features.parquet`,
`finalists.json`, the autoresearch harness, torch/CUDA + vectorbt working. Waiting on the canonical +4-day
refresh; then start at step 1.
