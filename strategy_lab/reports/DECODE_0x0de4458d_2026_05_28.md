# Wallet Decode: 0x0de4458d — BTC 5m — 2026-05-28

## One-line strategy

**Chainlink-divergence momentum taker**: fires when binance spot deviates EXTREMELY from chainlink RTDS (cl_basis < 9 bps → buy Up; cl_basis > 12 bps → buy Down), entering in first 33s of each 5m window.

---

## Key stats

| Metric | Value |
|---|---|
| Fires | 297 |
| Overall WR | 69.0% |
| Mean entry_px | 0.594 |
| Date span | May 19–27 (~7 days, ~42 fires/day) |
| fire_offset_s median | 33s (fast entry) |
| Up fires | 140 (47%) |
| Down fires | 157 (53%) |

---

## Entry price tier breakdown

| Tier | N | WR | Comment |
|---|---|---|---|
| cheap < 0.5 | 61 (20.5%) | 34.4% | Losing sub-population — drags overall WR |
| mid 0.5–0.85 | 236 (79.5%) | **78.0%** | Core real-edge tier |
| late > 0.85 | 0 | — | No late entries |

**The cheap tier is the dominant drag.** Overall WR 69% vs mid-tier WR 78%. The cheap tier fires with near-random direction (Up: 45.9%, Down: 54.1%) and achieves only 34% WR (vs ~50% random baseline). These may represent misfires or a separate sub-strategy the wallet runs in parallel.

---

## (a) Direction picker

### Primary signal: `cl_basis_bps` (Cohen's d = 0.996 — dominant)

```
cl_basis_bps = (binance_spot_px - chainlink_RTDS_px) / chainlink_RTDS_px × 10000
```

**Rule:**
- `cl_basis < 9 bps` → buy **Up** (BTC barely above chainlink — early momentum, room to run)
- `cl_basis > 12 bps` → buy **Down** (BTC far above chainlink — chainlink lags; reversion or continuation in chainlink direction)
- `cl_basis 9–12 bps` → neutral zone, lower WR (0.602)

**Performance by cl_basis zone:**

| Zone | N | WR | % of fires actually Up |
|---|---|---|---|
| low < 9 bps | 90 | 76.7% | 77.8% |
| mid 9–12 bps | 113 | 60.2% | 43.4% |
| high > 12 bps | 94 | 72.3% | 22.3% (= 77.7% Down) |

Threshold rule `cl_basis < 10.37 => Up`: 70.4% direction agreement overall, 76.7% on mid tier.

### Secondary signals (collinear with cl_basis)

| Signal | Agreement with direction |
|---|---|
| MACD > 0 => Up | 58.2% (mid tier) |
| ret_15m > 0 => Up | 56.8% (mid tier) |
| ret_30m > 0 => Up | 58.2% (mid tier) |

Mid-tier Up fires: MACD = +8.8, ret_15m = +3.45 bps (bullish momentum)
Mid-tier Down fires: MACD = −3.9, ret_15m = −1.37 bps (bearish momentum)

**MACD and returns are collinear with cl_basis** — they measure the same underlying BTC momentum direction. cl_basis is the primary operational signal.

### Verdict: MOMENTUM (not contrarian)

The wallet buys the momentum direction as captured by the binance-vs-chainlink divergence:
- BTC running up → cl_basis LOW (chainlink catching up slowly) → buy Up
- BTC running down → cl_basis HIGH (chainlink lags the prior high) → buy Down

This is NOT contrarian. The cheap tier shows mixed/contrarian-ish behaviour (Up when MACD positive but entry_px=0.40), but it has 34% WR and should be treated as a separate misbehaving component.

---

## (b) Slug selection

**Engagement rate:** 19.8% of available slugs (297 / 1500 control pool).

**Primary selection criterion: extreme cl_basis.** The wallet massively overweights slugs with extreme cl_basis divergence:

| cl_basis zone | Engaged vs control ratio |
|---|---|
| low < 9 bps | **3.82x overweighted** |
| mid 9–12 bps | 0.58x (actively avoided) |
| high > 12 bps | 1.20x overweighted |

The wallet skips neutral-market windows (cl_basis 9–12, most common in control) and fires preferentially when the binance-chainlink gap is at an extreme. This IS the slug selection signal.

**UTC hour preferences:**

| Pattern | UTC hours | Lift |
|---|---|---|
| Preferred | 22:00, 16:00, 12:00, 17:00, 20:00 | +33% to +80% |
| Avoided | 08:00–09:00, 00:00, 14:00 | −38% to −48% |

Fire timing: **median 33s** into 300s window — fires aggressively at the start of the slot.

---

## (c) Is the edge real?

**Yes — strongly statistically significant:**

| Test | p-value |
|---|---|
| Mid-tier WR (78%) vs 50% random | 8.8 × 10⁻¹⁹ |
| Mid-tier WR vs control baseline (40.3% in low cl zone) | 2.5 × 10⁻¹² |

**Control outcome baselines** (unengaged slugs by cl_basis zone):
- cl_basis < 9: only 40.3% Up outcome (market prices Up at a DISCOUNT when cl_basis is low)
- cl_basis > 12: 49.9% Up outcome (essentially coin-flip)

The wallet achieves 76.7% WR buying Up in the cl_basis < 9 zone where control shows only 40.3% Up resolution. This is the core alpha: the wallet correctly identifies that low cl_basis slugs will resolve Up at much higher rates than the CLOB prices suggest.

Similarly, buying Down in the cl_basis > 12 zone achieves 72.3% WR against a 50% baseline.

**Win vs loss separability (mid tier):**

| Feature | Win mean | Loss mean | d |
|---|---|---|---|
| ret_1m | 0.137 | −0.518 | 0.177 |
| ret_15m | 1.37 bps | −0.67 bps | 0.128 |
| macd_hist | 0.65 | −0.49 | 0.158 |
| cl_basis | 10.61 | 10.59 | 0.006 |

Win/loss separation is **weak** — the edge comes from correct entry selection (cl_basis extreme zone), not from a separable win predictor after entry.

---

## Strategy mechanics summary

```
IF cl_basis < 9 bps:
    buy UP token at ~0.62 entry (priced cheap — market underestimates Up probability)
    WR = 76.7%

IF cl_basis > 12 bps:
    buy DOWN token at ~0.57 entry
    WR = 72.3%

IF cl_basis 9–12 bps:
    SKIP (neutral zone, WR = 60%, not profitable enough)

Timing: fire at ws_s + 12–65s (fast, median 33s)
Hours: prefer 12:00, 16:00–17:00, 20:00–22:00 UTC
```

**The wallet's alpha**: Polymarket's CLOB misprices outcomes when the binance-chainlink divergence is extreme. At low cl_basis, Up tokens are priced at ~0.40 (cheap) despite resolving Up 76.7% of the time. At high cl_basis, Down tokens are priced cheap despite resolving Down ~73% of the time.

---

## Cheap tier analysis

The 61 cheap-tier fires (entry_px < 0.50) have WR 34.4% — a net loser. They have:
- Near-random direction allocation (Up 45.9%, Down 54.1%)
- MACD near 0 (mean = −1.8, much lower than mid-tier)
- Slightly later firing (mean offset 61.5s vs 54.9s)
- Contradictory signals: cheap-Up fires have *positive* MACD (+6.2) but still buy Up at 0.40 — this is "buying expensive side cheap" but with 25% WR — a losing bet.

**Most likely interpretation:** The cheap tier fires happen on slugs where the wallet's cl_basis signal fires but the market has ALREADY moved (cl_basis shifted after the signal but before fill). The 0.40 entry price reflects getting a bad fill on the wrong side of a fast-moving market. These are essentially misfires or fill artifacts.

**Recommendation for replication:** Filter `entry_px >= 0.50` to target only the 78% WR mid-tier behaviour.

---

## Deployability

| Dimension | Assessment |
|---|---|
| Edge real | YES — p < 10⁻¹² vs control baseline |
| Mechanism clear | YES — cl_basis extremeness selects slugs, direction from cl_basis sign |
| Reproducible | MOSTLY — cl_basis is computable from binance + chainlink RTDS at fire time |
| Data requirements | Binance spot (real-time), Chainlink RTDS (~same latency as production), L25 book |
| Known gap | Cheap-tier misfires not fully explained (20.5% of fires, destroys WR) |
| Competition | 42 fires/day, fast entry (33s) — needs low-latency infrastructure |
| Risk | Signal degrades if binance-chainlink latency alignment changes |

**Deployability verdict: MEDIUM.** The core edge is real and reproducible. The cl_basis signal is computable in production (same data sources already live). The 78% mid-tier WR at 42 fires/day is a meaningful edge. Main risk is the 20.5% cheap-tier contamination — a production implementation needs a strict `entry_px_min >= 0.50` guard (or equivalent cl_basis extreme threshold) to replicate the 78% tier rather than the 69% blended WR.

---

*Generated: 2026-05-28 | Harness: trigger_decode_harness.py | Cache: strategy_lab/wallet_hunt/cache/0x0de4458d/*
