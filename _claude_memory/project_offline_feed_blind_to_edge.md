---
name: project_offline_feed_blind_to_edge
description: "canonical L25 is ~92-96% FAITHFUL on fills (corrected) — the earlier 'feed blind to ~half the fills' was a join bug; collector is dedup-on-change ~1Hz, trade tape is a data-API 5s poll so NEVER join book↔trade on trade.local_timestamp_us"
metadata: 
  node_type: memory
  type: project
  originSessionId: 05cced9b-6087-4f55-b19f-1a29add92555
---

**CORRECTED 2026-06-16** (full investigation: local pipeline → VPS3 storedata → collector code → re-test; `L25_FEED_GAP_DIAGNOSIS_2026_06_16.md`). An earlier same-day claim that the canonical L25 was "structurally blind to ~half the fills" was **WRONG — a measurement bug of mine.** Retained here as a correction so nobody re-panics about it.

**What's actually true:**
- The canonical L25 is **~92–96% faithful on fills.** Corrected steady-state feed loss (exchange-time join, ±3s) = **3.6% count / 7.6% volume any-side; 7.7% / 12.8% rel-side.** The residual is top-25 depth truncation (deep walks) + occasional WS drops.
- The collector (`/opt/storedata/src/storedata/collectors/polymarket.py`, VPS3 `storedata-collector.service`) writes `orderbook_snapshots_v2` with **dedup-on-change** (suppresses identical consecutive top-25 states). So the persisted cadence is **~1 Hz event-driven (NOT 10 Hz)**, and the multi-second snapshot "gaps" are mostly the book being UNCHANGED (last snapshot stays valid) — **benign, not missing data.**
- Local pull/convert/merge are **faithful** (VPS3 source 1624 rows vs local 1620 for the test slug). No local pipeline bug.

**CORRECTION 2 (operator pushback "collector gets 10Hz" → re-read the handlers — THE REAL GAP):** the collector DOES receive a high-freq stream and DISCARDS it. `_handle_price_change` = "Phase 15-05: NO DB write" — the `price_change` DELTAS (collector comment: "fire hundreds of times/sec") are received but only fire a callback, never persisted; only periodic full `book` snapshots are written. Deduper is lossless (blake2b exact top-25; "10-20× row-reduction vs naive every-event writes" = inbound is ~10-20× the persisted ~1-2Hz). **So: persisted full-book (~1-2Hz) is ~92% faithful for COARSE fills (directional backtests fine), BUT order-level book evolution — queue adds/cancels, sub-100ms transients — is GONE. That delta stream is exactly what maker/queue modeling (b945 ladder, sum-pair maker capture) needs.** The ~8% residual invisible fills ARE those between-snapshot transients. FIX = re-enable scoped `price_change` delta persistence on the VPS3 collector (`orderbook_deltas_v2`, crypto tokens only, keep full-book as keyframes → reconstruct ~10Hz offline); spec in `L25_FEED_GAP_DIAGNOSIS_2026_06_16.md` §8. Storedata-agent domain. So: the feed isn't "blind/broken" but it IS throwing away the queue-dynamics data we need for the maker thread.

**PHASE-2 CONSUMPTION PIPELINE BUILT 2026-06-16 (ready for when the deltas land):** `load_orderbook_deltas(asset,slugs)` + `reconstruct_book_10hz(keyframe,deltas)` (keyframe-reset + apply deltas → delta-resolution book, same shape as the L25 loader; unit-tested `strategy_lab/directional/_test_reconstruct_book.py` ✓) added to `data/v4/canonical/load.py`; pull/merge `migration_2026_06_16/{pull_deltas.sh,merge_deltas.py}` → `canonical/orderbook_deltas/{asset}.parquet`; sim `strategy_lab/directional/phase2_maker_queue_sim.py` (FIFO maker queue, delta-book vs 1Hz-book flow-capture — answers the maker NO-GO of full-book ceiling ~4-7% vs b945 ~11.5%; runs when data exists, exits gracefully until then). Also: maker opportunity SIZED 2026-06-16 (`_maker_opportunity_2026_06_16.py`) — taker flow ~$30k/slug btc-15m; 84% at-existing-level (rest+queue patience), 11% inside-spread (needs being-first = the speed/infra edge), 5% deep → reconciles "be first" (article) vs "queue discipline > speed" (06-13 audit): speed is the ~11% minority.

**🔴 WS price_change FRAME FORMAT + TVRUST PARSE BUG (2026-06-16):** the Polymarket CLOB WS `price_change` event is **`price_changes[]` with a PER-CHANGE `asset_id`** (storedata agent verified live; format CHANGED from the old `changes[]` + message-level asset_id). My storedata spec had the old (wrong) shape → agent fixed it. **TVRUST `crates/tv-feeds/src/poly_book.rs` (`:109,:117,:143`) ALSO has the stale parse** (`evt.get("changes")` + one message-level `asset_id`) → very likely produces **0 price_change events → the live BookState only updates on ~1Hz full `book` snapshots → STALE book for quoting**, silently degrading EVERY TVRUST poly strategy (sniper/scalp/ladder/V2) since the format changed. FIX = accept both `price_changes`/`changes` keys + route/enrich each change by its OWN asset_id (mirror storedata's fix); verify vs the live frame. Architecture note: TV needs the live in-memory book to EXECUTE (it does — `apply_inner` handles `BookEvent::PriceChange`); it does NOT persist a tape (that's storedata) → the TVRUST delta tick-recorder spec was RETRACTED.

**✅ VERIFIED LIVE ON VPS3 2026-06-16 (direct psql):** snapshots_v2 = **1.15-1.45/s** (btc-15m) = confirmed ~1-2Hz (NOT 10Hz; storedata snapshot book == canonical, both ~1-2Hz). The agent's `orderbook_deltas_v2` is **LIVE + WORKING**: schema correct (13 cols), real-time (0.66s stale), capturing the high-freq stream — btc-15m **145 deltas/s per outcome** (eth 71, sol 43), parse correct (side normalized bid/ask, 6 distinct asset_ids = per-change asset_id ✓), 15m-scoped per the volume decision. **Head-to-head same slug/5min: 77,926 deltas vs 890 snapshots = ~88× richer.** So the order-level stream is ~145-290/s (far MORE than "10Hz"); the snapshot dedup just collapsed it to ~1-2Hz. The maker/queue (Phase-2) data is now flowing → run the Phase-2 sim after a few days accrue.

**🔴 REGRESSED 2026-06-29 — `orderbook_deltas_v2` writes BROKE (table EMPTY despite the collector receiving ~434k price_change events/cycle; snapshots fresh, 15m markets active, gates pass).** Worked ~Jun 16 (1M pulled), broke after the agent's gate/scope/per-change-asset_id iteration; ~13 days of delta data LOST. Localized to the per-change `asset_id` path: (1) `change["asset_id"]` not matching `_asset_info`/`_snapshot_received` keys (book channel's asset_id) → all skipped, or (2) `_flush_delta_buffer` not wired. Diagnosis + 1-line test (`log _delta_count`) in `STOREDATA_DELTA_WRITE_REGRESSION_2026_06_29.md`. **LESSON: never assume deltas are flowing — verify `orderbook_deltas_v2` has fresh rows before any Phase-2 analysis.** Also: real date is 2026-06-29 (session spans Jun 16→29); canonical data still ends ~Jun 15.

**🟢 IRELAND LADDER+RACER FULL DECODE — 13.4 DAYS (Jun 16→30, `IRELAND_LADDER_FULL_DECODE_HANDOFF_2026_06_30.md`; supersedes the 6-day `..._RACER_6DAY_2026_06_29.md` which was a slice w/ 2 misreads).** `vps_ireland` `tradingvenue_rust.trading.events`, sleeve `poly_ladder_btc_15m`, PAPER. **1,161 windows, 98% TRADED.** Two-sided maker ladder (rest both sides → capture taker sell-flow → lock pairs that fill at sum<1, hold to chainlink; residual = unhedged remainder). **The offline NO-GO is OVERTURNED: pair_frac 0.80 (vs offline 0.29 ceiling).** **Matched-pair PnL = +$1,611 LOCKED & OUTCOME-INDEPENDENT** (verified corr 1.000: `net_paired = paired_sh·(1−pvs) + rebate`; you hold BOTH legs so WHO wins is irrelevant — real exec arb, ~$120/day on $5 clips) + rebate $118. pvs mean 0.956 / median 0.967; latency recv→apply p50 **61.6µs**; 4-conn racer (15.7M dedup, 468M level-updates applied = book delta-fresh, 0 recorder drops). **CORRECTIONS to the 6-day report:** "warmup 20%" was TICK-level (window-level 98.7% pass — coverage fine); "+$623 paired-only/true-net-unknown" → full 13.4d is **+$1,611 and it's locked arb, not a fragile directional number**. **The ONE unmeasured number = residual PnL** (10,634 directional sh, 9.34/win; entry-vwap+outcome NOT logged, box's other sleeves log `outcome:null` so no backfill). Bounded est −$1.15k…+$0.96k, **most-likely ~neutral** (residual bought ~0.48, btc-15m ~coinflip → +$120) → TRUE net very likely stays clearly positive, CANNOT plausibly flip negative on these numbers. **Caveats:** flow_capture only **1.7%** (vs b945 11.5% — thin/few-level quoting, the 5-10× scale lever); **33% of windows lock a pvs>1 LOSS** (−$442 total — gateable: refuse to complete a pair at running-sum≥0.99). residual_side 635up/499dn = net-long btc beta. **Go-live gates: G1 log residual_pnl (filled_up/dn_vwap+outcome) → net CI>0 [the deciding fix]; G2 deploy tv-watchdog; G3 test pvs≥0.99 gate; then $1-5 live btc-15m judged by live wallet.** Mechanism live-validated + paper-positive; one telemetry fix from confirmed-deploy. **2026-06-30 follow-up:** full `tradingvenue_rust` DB archived locally → `D:\global_data\ireland_archive\{tradingvenue_rust_2026-06-30.dump (93MB pg_dump -Fc), trading_events_2026-06-30.tsv.gz (50.8MB, 1.636M rows)}` (verified, sha256 in spec). **v2 fresh-engine spec for TV agent = `TV_AGENT_SPEC_LADDER_V2_RESIDUAL_PVSGATE_FRESH_2026_06_30.md`:** new DB `tradingvenue_rust_v2` + sleeve `poly_ladder_btc_15m_v2` (no v1 mixing); G1 = log filled_up/dn_vwap+outcome+residual_pnl_usd+total_net_usd (winner-only fee, residual marked to chainlink); G3 = `TV_LADDER_PAIR_MAX_SUM=0.99` cap (opposite bid ≤ max_sum−filled_side_vwap, kills the 33% pvs>1 locked losses, excess→residual); same clip/levels/markets as v1 for clean A/B; delete v1 (DROP DATABASE or TRUNCATE June partition) AFTER backup+v2 confirmed (Ireland 53G free, not urgent). Analysis pipeline `strategy_lab/directional/_ireland_6day/analyze.py` runs unchanged on v2. **🔴 v2 FIRST RESULTS 2026-07-01 (31h, 104 clean windows, `IRELAND_LADDER_V2_FIRST_RESULTS_2026_07_01.md`): G1 answered it — residual drag −$2.24/win CI[−2.77,−1.69] > paired edge +$1.58/win → TOTAL NET −$0.687/win CI[−1.30,−0.06], P(>0)=1.7% = NO-GO as configured.** Mechanism = adverse selection at −5.5σ: the heavier-filled (residual) side wins only 14.7% (big residuals 6.9%; lighter side wins 85.3%); −$0.241/sh held. `outcome` ground-truthed 3/3 vs Binance. G3 gate WORKS (0/96 pvs>0.99 vs v1 33%>1; pvs 0.956→0.859; paired $/day $111→$142) but halves fills (pair_frac 0.80→0.60, flow_capture 0.8%). **Counterfactual on same tape: flatten residual at ≤0.05/sh cost → +$1.21–1.62/win POSITIVE → v3 = residual management (maker-recycle heavy side + T−60s taker-flatten backstop), the go/no-go lever.** Deploy used fallback path (same DB, sleeve `poly_ladder_btc_15m_v2`; v1 not yet deleted). Minor bug: `pair_gate_bound_sh` never counts. Bank: maker-fill imbalance as directional signal (85% lighter-side) — untested, n small. ALSO: VPS3 `orderbook_deltas_v2` broke AGAIN (7.15M rows then stale since Jun 30 18:24+02) — storedata agent re-diagnose. **🔴 RUST IMPL AUDIT 2026-07-01 (`IRELAND_RUST_IMPL_AUDIT_2026_07_01.md`): (a) the Rust resolve tape books HOLD-to-resolution PnL for ALL sleeves (resolver.rs `slot_resolution_pnl`; scalp losses = −$5.000 exact) — the ScalpExit sell loop is spawned (sniper.rs:1418) but its result reaches NO event → the Rust `shadow_scalp_exit_btc_5m_d3_v1` −0.074/tr (n=455) is a favorite-hold counterfactual, NOT scalp-edge parity — never judge the exit-scalp from Rust resolve rows until exit PnL is wired; (b) entry_band (0,0.55) enforced ONLY on the live path (sniper.rs:1364) — paper fills median 0.63 both dirs = out-of-band population; (c) port carries stale tp0.65/stop0.10 knobs vs operator-final PURE+60s; (d) `sumpair_osc.rs` CODE-COMPLETE Jun 29 but `TV_SUMPAIR_OSC_ENABLED` never set on Ireland → zero events, one env flag away; (e) tv-watchdog NOT deployed; (f) endgame feed staleness: active book_age 54-150ms early/mid window but 0.9→2.7s in the last 3 deciles (the >15¢ reject gate, 42.6k rejects, suspect) — exactly where a T−60s residual backstop would execute; (g) fleet tape Jun 11→Jul 1: ETH V10 +0.33/tr matches family (parity ✓), btc-15m snipers bleed (poly −$153 n=581, kalshi −$50), kalshi_scalp resolves pnl=NULL.**

**🧠 SCALP-MOMENT DATA GAP (2026-07-02, `SCALP_RETRO_AND_TRUE_TAPE_2026_07_02.md`): the +5s/+65s early-window microstructure the scalp trades EXISTS ONLY in the engines' own ws_mirror books — storedata subscribes each 5m market ~+117s median (canonical: 2% of 5m slugs have books by +5s, 31% by +65s) → retro-computing scalp fills/exits from storedata/canonical is STRUCTURALLY IMPOSSIBLE; engine tapes (`poly_updown_scalp_exit` events) are the only truth. Fix if ever needed offline: collector pre-subscribe at market discovery (markets listable ~24h early per b945).** TRUE TAPE Jun 11→29: VPS3 Python shadow btc_5m_d3 **+$0.783/tr CI95[+0.36,+1.21] n=77 WR65% band-holds-100% trigger=time60 — edge ALIVE, consistent w/ corrected OOS +0.91**; weekly $/tr 0.04→0.42→0.96→0.65 (regime-tracking). Ireland live $1: n=9 +0.048 (CI incl 0, keep accruing to ≥200). Live directional fleet still bleeds (btc15m sniper −8.30/98tr; eth v7/v8 negative small-n); scalp = only CI>0 live strategy. Rust twin −0.074 fully explained by missing band+exit (fix spec confirmed necessary; retro-repair impossible). **🔴 LIVE-READINESS AUDIT 2026-07-02 (`SCALP_LIVE_READINESS_AUDIT_2026_07_02.md`, 4-agent source+DB audit): (1) THE PYTHON LIVE PATH NEVER SELLS — `maybe_scalp_exit` has no live-order branch in taker mode: live = real $1 entry + SIMULATED +60s exit; real tokens ride to resolution (the 17 poly_redeemed) → live money ran WITHOUT the edge and drained the wallet to $1.77 (76+ CLOB rejections 'not enough balance' Jun 26+, journalctl-only, invisible in trading.events). Implement the live taker sell BEFORE any deposit. (2) Shadow verified honest: arithmetic exact 1e-15, $5.000 stake, band/δ/trigger all clean, sell walks realistic (4/77 deeper never better), sell-leg fee 0.07-curve charged UNCONDITIONALLY (~$0.14/tr, conservative, stale '$0 proxy' docstring), latency 0ms (small optimism). (3) Live stake $1 GLOBAL w/ hard cap SNIPER_V5_LIVE_MAX_NOTIONAL=$2.00 (py:97) — $1 clips ≈2 shares = below ~5-share venue min; marketable-limit 0.99 needs collateral ≈2× notional. (4) Fire gap = 2 causes: wallet empty (Jun26+) + unexplained Ireland-vs-VPS3 gate divergence Jun11-25 (VPS3 79 all-pass vs Ireland ~0, same cadence — book-mirror variance, follow up). (5) MIN CAPITAL: $5 clips (validated size) = fund ~$150; expected +$101/mo; ≥200-fire gate ≈45-50d; $25 clips ~$300, +$505/mo, depth-safe (0.6% util), ONLY after gate. Max concurrent = 1. Plan: live-sell patch → $5 notional + cap raise + allowlist=btc_5m_d3 only + rejection telemetry → fund $150 → 200 fires → judge live CI.**

**THE GOTCHA that caused the bug (bank this):** the trade tape (`trades_polymarket`, `trades_v2`) is **NOT the WS — it's a data-API poll every 5s.** On trade rows, `timestamp_us` = exchange match time (**second-granular**), `local_timestamp_us` = collector poll/WRITE time (lags real trade by **p50 0.1s, p90 337s, hours on backfill**). I joined T1 on `local_timestamp_us` → garbage. **For ANY book↔trade microstructure join, use `timestamp_us` (exchange) on BOTH, with ≥±1s window for the trade second-rounding.** (Book `timestamp_us` is source ms; clocks align to p50 0.03s offset.)

**How to apply:** offline L25 fill verdicts are MORE trustworthy than the 06-16-morning panic implied (feed ~92–96% faithful). The "speed-flat was an offline artifact / build the racer because our data is blind" justification (`SUMPAIR_B945_ERROR_AUDIT §3b, Finding #2`) is **withdrawn** — the racer's case reverts to the original b945 LIVE-execution thesis, which the feed-loss test does not speak to. Minor real items: relabel "10Hz"→"~1Hz dedup-on-change"; collector subscribes ~116s late (median) but ~0% of fills land that early so low impact; `l25_merge_safe.py` uses a monotonic `ts>max` filter (latent poison-row risk, not firing). Links [[project_sumpair_arb_dead]], [[project_b945_thread_parked]], [[project_5lens_audit_2026_06_12]].
