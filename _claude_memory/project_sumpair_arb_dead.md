---
name: project_sumpair_arb_dead
description: "sum-pair arb (buy Up+Down when ask-sum<1, hold) tested 2026-06-13 — DEAD as taker (dips revert <100ms before 85ms order lands, −5 to −11¢/pair SIG-NEG all coins); only sub-100ms maker capture unclosed"
metadata: 
  node_type: memory
  type: project
  originSessionId: 95da9a9e-f870-4e84-8fd8-e3dfce1d3760
---

Ran the pre-registered atomic taker sum-pair arb test (paper 2508.03474 "Market Rebalancing", 0xSurferX/Luoye "binary hedging"). `sumpair_arb_t1_2026_06_13.py`, full report `SUMPAIR_ARB_VERDICT_AND_SHADOW_PLAN_2026_06_13.md`.

**DEAD as a taker.** Causal first-cross sum_ask<θ → fill at L25 snapshot +85ms (measured live latency) → real depth walk both legs → true chainlink resolution → winner-only 0.07 fee. Every cell (BTC/ETH/SOL × 5m/15m × θ 0.99-0.95, 1400 slugs each) **SIG-NEGATIVE**: pooled lat $/pair −0.070 (θ0.99) to −0.082 (θ0.95). The arb math is real (opt/instant fill turns +0 below 0.97) but **the dip is a single-snapshot <100ms transient that reverts before the 85ms order lands** → you fill at the ~1.01 overround → lose. Deeper dip = faster revert (haircut +0.056→+0.111). SOL worst (−11¢, thinnest book).

Confirms: ce25 DEPLOY-NO (its profit = winner-leg resolution recovery, CLOB-only net −$9,117, NOT arb); b945 maker SIG-NEG (adverse selection); LEG2 −0.9 lockstep repricing. Combinatorial arb (paper's other type) N/A — our crypto markets are single-condition.

**Only unclosed door:** sub-100ms resting-MAKER capture (historical 10Hz can't see it; b945 sim says negative). Worth at most a $0 observe-only shadow (`sum_pair_monitor` sleeve: log live dip duration at full WS freq + virtual taker/maker fills + chainlink resolution). Promotion to $1 live maker only if both-legs-fill ≥30%, net CI>0, ex-top2 robust over ≥4wk/≥200 pairs. Else file fully dead.

**🟢 UPDATE 2026-06-14 — "DEAD" was too absolute. SIGNAL-GATED OSCILLATION-HARVEST is a REAL (thin) edge** (`SUMPAIR_SIGNAL_GATED_2026_06_13.md`, `_sumpair_signal_oscillation_harvest.py`). Three signal-gated variants tested using the DEPLOYED lag-scalp signal (Binance bar-END |ret|≥3bp, ev<0.55, +85ms): V1 instant-pair DEAD (−$2.09/slug, pays overround, sum 1.015); V3 lean+hedge DEAD (hedge caps winner upside, doesn't beat +60s scalp). **V2 oscillation-harvest = EDGE-FOUND: buy EACH side at its OWN causal lag-dip across the window, accumulate, pair=min(sh_up,sh_dn) hold to chainlink → paired sum 0.73 median, 92.6% sub-1.** Survives ALL artifact checks: bar-END causality (leaky bar-start does NOT raise PnL), 85ms latency, chainlink settle, and the LINCHPIN markout (filled cheap ask RISES +8¢/30s = genuine lag, OPPOSITE of the taker sub-100ms revert). BEATS the deployed +60s scalp (+1.95/slug paired diff, CI excl 0). **BUT headline +$2.24/slug is ~4× inflated by infinite-refill (resolve_size size==0→DEEP); honest 1-clip-per-side = +$0.52/slug OOS [+0.36,+0.68], 5m-ONLY (15m straddles 0), 58% of windows lose; locked-pair component positive IS+OOS, directional residual −$1.19 is the drag.** DEPTH-REALISM test running 2026-06-14 (`_sumpair_v2_depth_realism.py`) to bound the true magnitude between +0.52 (1 clip) and +2.24 + test residual-scalp-exit refinement. **Why:** the operator was RIGHT — a signal-gated sum-pair edge exists (the taker-simultaneous DEAD verdict above still holds; the NEW path is per-side lag-gated accumulation). **How to apply:** taker-simultaneous still never; but the V2 oscillation-harvest (5m, 1-clip, lag-gated, hold matched pair + scalp-exit residual) is the live-shadow candidate — judge by live wallet CI, depth-realism pending.

**🟢 UPSIDE/DEPTH DONE 2026-06-15 (`_sumpair_v2_upside.py`, spec §3b):** real 25-level depth supports MEDIAN 2 clips/side (p25=1,p75=4; depth_short=0 → book carries the $5 clips, cap is a choice). Max-clip net/slug curve (real-depth, OOS): 1→+0.53, 2→+1.16, 3→+1.70, ∞→+2.97. **BUT the MEDIAN is −$5 and ~72% of fired slugs are SINGLE-LEG losers (only ~28% both-fill) — the positive mean is TAIL-DRIVEN by matched pairs; this run HELD the residual (pessimistic).** The residual-scalp-exit (sell unpaired leg +60s) is the fix for the −$5 median and is NOT yet backtested → measure LIVE in Stage 0 (log hold vs scalp-exit residual). Deployable: start MAX_CLIPS=1 (floor +$0.52 significant), raise to 2-3 (~+$1.2-1.7 mean) only after live shadow shows residual-exit lifts the median. TV agent spec ready: `TV_AGENT_SPEC_SUMPAIR_OSC_HARVEST_2026_06_14.md`. ⚠️ box heavily contended (other python procs) — offline runs ~4.5× slow; prefer live-shadow measurement over more offline.

**🟢 RESIDUAL-EXIT RESOLVED 2026-06-16 (`_sumpair_v2_depth_realism.py` finished + reported, `SUMPAIR_V2_DEPTH_REALISM_2026_06_14.md`):** the 06-15 open question ("scalp-exit fix for the −$5 median NOT yet backtested") is now ANSWERED offline. Full 25-level realistic-depth walk (per-level carry, no DEEP) confirms real depth = ~9 clips/snapshot/side → multi-clip is NOT a refill artifact; ARM A (hold) OOS net/slug 1→+0.64, 2→+1.09, 4→+1.75, ∞→+2.45 (all CI>0). **KEY: scalp-residual (ARM B) is the DEPLOYABLE form, NOT hold.** 74% of fires are single-leg; HOLD has higher MEAN (+0.64/+2.45) but **median −$5, 36-38% win = untradeable**; SCALP the unmatched leg +60s → median −$5→−$0.35, win 38%→44%, mean +0.40 (1-clip, CI[+0.26,+0.55]) / +1.77 (unlim). Per-coin ARM B: BTC strong (+0.91/+3.32), ETH solid (+0.41/+2.23), SOL straddles 0 (DROP). Markout +1.9/+3.7/+2.5¢ (lag real but +30s CI straddles 0, tail-driven). **Deployable: BTC/ETH 5m, pair-hold + SCALP residual (NOT hold), MAX_CLIPS=1 floor +0.40 → multi-clip ~+1.8 if depth regenerates. The ONLY open Q = inter-fire liquidity regeneration (does the lagging quote refill after you lift it) — L25 can't see it → live shadow only.** Encoding bug in the script (→ char crashed cp1252 print) fixed via stdout.reconfigure.

**🟠 META-CORRECTION 2026-06-16 (0xSurferX new article "I'm 21, my bot hit $21k" + TVRUST infra audit → `SUMPAIR_B945_ERROR_AUDIT_AND_TEST_PLAN_2026_06_16.md` + `TV_AGENT_SPEC_TVRUST_MOAT_INFRA_2026_06_16.md`):** the "DEAD/offline" verdicts above AND the b945 "speed flat / queue-priority is NOT the lever" re-audit BOTH rest on a SINGLE-connection 10Hz L25 feed that is structurally blind to the sub-100ms book churn where the edge lives. The "speed flat" sweep varied REACTION latency on a fixed lossy tape; it never tested FEED COMPLETENESS (a different axis — the racer captures levels the one collector never saw). So the infra-moat is **UNRESOLVED, not closed**, and resolvable only with the instrument we never built. Decisive cheap offline gate = **T1 feed-loss audit**: fraction of real taker prints that execute at price levels our snapshots never showed; **≥~20% ⇒ feed blind ⇒ build the racer; <5% ⇒ feed fine, offline verdicts stand.** Economics survive (Q≈4 GLT, γ=0.05 AS skew, winner-only fee, no-split, post-resolution merge) — only the feed-race dismissal is re-opened. See also [[project_offline_feed_blind_to_edge]]. Next: run T1, then build TVRUST moat instrument (N-conn racer + own tick tape + per-hop latency tape) → measure flow-capture/pair-fraction LIVE (L1–L3); watchdog must be deployed before any live arm.

**🟠 V2 FILL-MODEL FRAGILITY (2026-06-16, after fixing the L25 odd-level depth corruption [[project_l25_level_corruption]]; `_v2_verify_diff.py` + `_sumpair_v2_upside.py` re-run):** the +0.52/+0.40 floor was validated on LEVEL-0 entry_fill (partial-fill-at-best), which the corruption NEVER touched — differential shows 0.000 change corrected-vs-corrupted, so the floor is NOT a corruption artifact. BUT a realistic full-$5 BOOK-WALK on the CORRECTED deep book = **−0.70/slug** (the corrupted realdepth had shown +0.52 — THAT was the artifact). They diverge because V2 fires at THIN lag-dip moments where level-0 can't fill $5 → partial-at-best (+0.52 but tiny size) vs walk (−0.70). **So +0.52 is UNCONFIRMED for real deployment.** Keep the V2 sleeve at $0 shadow (spec already says so), judge by the LIVE fill+PnL, no capital until the live wallet is positive; do NOT size on +0.52. (The L25 corruption only affected DEEP-book analyses; the deployed scalp/sniper use level-0 → unaffected.)

**🟢 SHORT-SIDE SCANNED + DEAD 2026-06-16 (`strategy_lab/directional/_t3_shortside_scan_2026_06_16.py`):** the overround SHORT side (sum_bid>1 → split+sell both legs; paper appendix H claimed "more profit") is RARE for our 2-outcome crypto — sum_bid>1.00 only ~0.1% of snapshots, >1.02 ~0.0%, depth ~$0.5 when it crosses; symmetric with the dead long side (sum_ask<1 ~0.1%). Books are efficient at the touch; the overround lives in the SPREAD, neither side crosses. Paper's claim was for COMBINATORIAL/election markets, NOT binary up/down. Don't re-run despite the paper. **LATENCY-CONFIRMED 2026-06-16 (`sumpair_short_t3_2026_06_16.py`, all 6 markets, 1400 slugs/cell, causal first-cross + 85ms fill + 2-leg taker fee):** lat $/set SIG-NEG every cell (pooled θ1.035 −0.098 CI[−0.104,−0.093]; SOL worst −0.13 to −0.15); `opt` (no-latency) barely +0 at θ≥1.035 (two-sided taker fee ~3.5¢ — DOUBLE the long side's winner-only ~1.75¢ — eats it). Same kill mechanism as the long side: sum_bid>1 spike reverts <100ms before the sell lands. DOUBLY dead (rare + uncapturable + worse fee). Closed.

**🟢 CE25/AGILE-SPACING FRESH DECODE 2026-07-02 (`CE25_FRESH_DECODE_2026_07_02.md`) — his signature EVOLVED toward OUR designs:** lifetime +$384.6k (+$84k in 3wk ≈ $4k/day), fresh 8.5h window fee-adj +$631 ≈ +$1.8k/day pace on only ~$2.4–5k working capital (~810 slugs/day pace, $123k/day vol, 8 mkts). CHANGED since Jun-12: open-racing GONE (14.7% first-60s vs 78%), now **multi-clip (mean 12 fills/slug, median $8.25) both-sides dip/passive accumulation across the whole window**, pair rate 86%, pair_frac only 0.55, pvs wide (41%<1.0, min 0.34), still 0 sells. = the ladder/V2 design class AT SCALE. Profit concentrates BTC (+$11.23/slug); SOL/XRP negative even for him. Fee drag 47% of gross IF taker (maker-vs-taker unresolved — classify his btc-15m fills vs OUR Ireland racer tape). **Implications: (1) his residual survives because dip-priced ≤~0.45 → cheap-side skew = v3 ladder design hint; (2) the MAX_CLIPS=1 cap now has live counter-evidence (his 12 clips/slug) — re-test multi-clip on clean delta data when storedata fixed; (3) capital is NOT the constraint to $1k+/day (he uses ~$3–5k), CAPTURE is.** API facts: data-api hard cap offset≤3000 (~3.5k rows, no time params — Alchemy for full days); lb-api windows only 1d|all; **gamma-api /events?slug= resolves ALL coins incl XRP** (vps3 market_resolutions_v2 ~50% gaps + 0% XRP — no XRP oracle on storedata).

**🔴🟢 HEAD-TO-HEAD OUR LADDER vs CE25, SAME DAYS (2026-07-02, `PAIRARB_LADDER_VS_CE25_HEADTOHEAD_2026_07_02.md`):** identical pair_frac (0.564 vs 0.55), WE pair cheaper (pvs 0.853 vs 1.017), we pay $0 fees vs his ~47%-of-gross — yet he +$1.8k/day, us −$76/day (n=142, −$0.91/win CI[−1.50,−0.32]). **The whole gap = fill selection: our at-touch resting bids absorb INFORMED window-scale sell flow (residual wins 14.1% vs 40% breakeven, −5.5σ) = we're exit liquidity; his clips are dip/overshoot-selected across the window (V2 markout +8¢/30s) → his residual wins ~half at cheap entries = HIS profit engine. We harvest the pair and eat the residual; he harvests the residual and tolerates the pair (pays >$1 for 41% of his pairs).** Fixes already in pipeline: ladder v3 residual mgmt (kills −$2.46 → net ≈ +$127/day btc-15m) + sumpair_osc dip-gated accumulation (= his fill selection). MERGED design (dip-gated two-sided + pair-lock + residual mgmt) = him, with our $0-fee maker advantage. **CLASSIFIED 2026-07-02 (820 fills vs pre-fill VPS3 books, age med 0.78s): HYBRID — maker 42.5%$ / inside 10.4% / taker 47.2%$ (robust); 5m more maker (63%$), 15m more taker (50%$). THE KEY: his maker bids rest BELOW the touch (med −1 tick, p10 −5) — deep spike-through fills, NOT at-touch → both halves of his flow are dip-selected. = our two engines combined: sumpair_osc (taker half) + ladder-with-deep-quotes (maker half, one param: quote-depth offset −1..−5 ticks). His true net higher than fee-adj est (only ~47% of $ pays fees) → up to ~$2.5k/day pace. XRP fills unclassifiable (storedata has no XRP books).**

**Why:** prevents re-testing taker sum-pair arb; the next person must target the maker-capture question or drop it.
**How to apply:** never deploy taker SIMULTANEOUS sum-pair; the $0 monitor shadow + the V2 oscillation-harvest live shadow are the sanctioned next steps. Links [[project_5lens_audit_2026_06_12]], [[project_b945_thread_parked]], [[project_synthetic_book_marginal]].
